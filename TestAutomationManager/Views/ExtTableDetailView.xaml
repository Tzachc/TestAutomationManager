<UserControl x:Class="TestAutomationManager.Views.ExtTableDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1100"
             Background="{StaticResource PrimaryBackgroundBrush}">

    <!-- Local styles just for this view -->
    <UserControl.Resources>

        <!-- Card container with rounded corners -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- DataGrid overall style - NOW EDITABLE -->
        <Style x:Key="CleanDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI, Calibri, Arial"/>
            <Setter Property="MinRowHeight" Value="38"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="True"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="VerticalGridLinesBrush" Value="#35FFFFFF"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#20FFFFFF"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#0AFFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        </Style>

        <!-- Column header: bigger, bolder, clearer with proper spacing -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="0,0,1,2"/>
            <Setter Property="Padding" Value="12,10,32,10"/>
            <!-- ⭐ FIXED: Added right padding for edit icon -->
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid Background="{TemplateBinding Background}">
                            <!-- Main Content Area -->
                            <ContentPresenter 
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Margin="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True"/>

                            <!-- ⭐ FIXED: Edit Icon in overlay - positioned absolutely -->
                            <Button x:Name="EditColumnButton"
                                Width="20" Height="20"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="EditColumnName_Click"
                                Tag="{TemplateBinding Content}"
                                ToolTip="Click to rename column"
                                Visibility="Collapsed"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,6,0">
                                <TextBlock Text="✏️" FontSize="11" Opacity="0.8"/>
                            </Button>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20FFFFFF"/>
                                <Setter TargetName="EditColumnButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Rows with better hover effect -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Margin" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1827A1FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#28379BFF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Cells - editable with visual feedback -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8,12,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#30FFFFFF"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border x:Name="CellBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter 
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            SnapsToDevicePixels="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Editing mode visual -->
                            <Trigger Property="IsEditing" Value="True">
                                <Setter TargetName="CellBorder" Property="Background" Value="#28539BF5"/>
                                <Setter TargetName="CellBorder" Property="BorderBrush" Value="{StaticResource PrimaryBlueBrush}"/>
                                <Setter TargetName="CellBorder" Property="BorderThickness" Value="2,2,2,2"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#22379BFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER / META -->
        <Border Style="{StaticResource CardBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock x:Name="TableNameText"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="•"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="8,0,0,0"/>
                        <TextBlock Text="Rows:"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="8,0,4,0"/>
                        <TextBlock x:Name="RowCountText"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <!-- ⭐ NEW: Editable indicator -->
                        <Border Background="{StaticResource SuccessBrush}"
                                CornerRadius="4"
                                Padding="6,2"
                                Margin="12,0,0,0">
                            <TextBlock Text="✏️ EDITABLE"
                                       FontSize="9"
                                       FontWeight="Bold"
                                       Foreground="White"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Test:"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="TestNameText"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="6,0,0,0"/>
                        <TextBlock Text="("
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="8,0,0,0"/>
                        <TextBlock Text="ID"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="TestIdText"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="4,0,0,0"/>
                        <TextBlock Text=")"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="•"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="8,0,8,0"/>
                        <TextBlock Text="Category:"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="CategoryText"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   Margin="6,0,0,0"/>
                        <!-- ⭐ NEW: Edit instructions -->
                        <TextBlock Text="💡 Tip: Double-click any cell to edit"
                                   Foreground="{StaticResource TextTertiaryBrush}"
                                   FontSize="10"
                                   FontStyle="Italic"
                                   Margin="20,0,0,0"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="RefreshButton"
                            Content="Refresh"
                            Padding="14,6"
                            Click="RefreshButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- GRID WRAPPER -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,12,0,0">
            <Grid>
                <!-- Loading / Empty / Error states -->
                <Grid x:Name="LoadingPanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Loading…" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Grid>

                <Grid x:Name="EmptyState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="No data in this table" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Grid>

                <Grid x:Name="ErrorState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="ErrorMessageText" FontSize="16" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap" TextAlignment="Center" Width="520"/>
                </Grid>

                <!-- THE DATAGRID - NOW EDITABLE -->
                <DataGrid x:Name="TableDataGrid"
                          Style="{StaticResource CleanDataGrid}"
                          Visibility="Collapsed"
                          ColumnWidth="SizeToCells"
                          CellEditEnding="TableDataGrid_CellEditEnding"
                          BeginningEdit="TableDataGrid_BeginningEdit">
                    <DataGrid.Resources>
                        <!-- Centered TextBlock for cell content with wrapping -->
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="4,2"/>
                            <Setter Property="LineHeight" Value="18"/>
                        </Style>

                        <!-- Editing controls -->
                        <Style TargetType="TextBox">
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            <Setter Property="Background" Value="{StaticResource PrimaryBackgroundBrush}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlueBrush}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="4"/>
                        </Style>
                    </DataGrid.Resources>
                </DataGrid>

            </Grid>
        </Border>

        <!-- STATUS LINE -->
        <TextBlock Grid.Row="2"
                   x:Name="StatusText"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   FontSize="12"
                   Opacity="0.9"
                   Margin="2,10,2,0"/>
    </Grid>
</UserControl>